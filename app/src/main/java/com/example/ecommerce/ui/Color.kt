package com.example.ecommerce.ui

import androidx.compose.ui.graphics.Color

// Base Palette (Original)
val Ebony = Color(0xFF1E201E)     // #1E201E (very dark)
val Charcoal = Color(0xFF3C3D37)  // #3C3D37 (dark gray/greenish)
val Sage = Color(0xFF697565)      // #697565 (medium gray/greenish)
val Cream = Color(0xFFECDFCC)     // #ECDFCC (light cream)

// Slightly Darker Variants
val EbonyDark = Color(0xFF141614)     // a bit darker than #1E201E
val CharcoalDark = Color(0xFF2C2D28)  // a bit darker than #3C3D37
val SageDark = Color(0xFF535E51)      // a bit darker than #697565
val CreamDark = Color(0xFFD6CCB8)     // a bit darker (less bright) than #ECDFCC

// Slightly Lighter Variants
val EbonyLight = Color(0xFF2C2E2C)    // a bit lighter than #1E201E
val CharcoalLight = Color(0xFF4C4D47) // a bit lighter than #3C3D37
val SageLight = Color(0xFF7E8A78)     // a bit lighter than #697565
val CreamLight = Color(0xFFF5F0E9)    // a bit lighter than #ECDFCC